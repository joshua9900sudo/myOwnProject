#include <stdio.h>

int main() {
	int a = 8000;
	//check the length of the address (which is a integer)
	leftshift = 	
	char b[] = a + '0';
	for (int i = 0;;i++) {
		printf("%c", char b[i]);
		scanf("");
		}
	return 0;
	}
